package com.mobdeve.group3.mco.catalogue

class CatalogueSpecies(
    id: String,
    name: String,
    imageId: Int
) {
    var id = id
        private set

    var name = name
        private set

    var imageId = imageId
        private set
}