package com.mobdeve.group3.mco

class Category(
    id: String,
    name: String,
    imageId: Int
) {
    var id = id
        private set

    var name = name
        private set

    var imageId = imageId
        private set
}